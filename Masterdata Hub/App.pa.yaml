App:
  Properties:
    OnStart: |
      =Set(gblMe, Lower(User().Email));
      Set(gblView, "mine");
      Set(gblDomain, "");
      Set(gblStatusMode, "open")
    StartScreen: |-
        =ScreenMdHub
    Theme: |-
        =PowerAppsTheme
